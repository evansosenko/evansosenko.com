{
  "engines": {
    "node": ">=18.12.0",
    "npm": ">= 8.1.0"
  },
  "standard": {
    "ignore": [
      "themes/default/assets/js/posthog.js"
    ]
  },
  "scripts": {
    "lint": "standard",
    "postlint": "stylelint '**/*.css'",
    "format": "standard --fix",
    "preformat": "prettier --write '**/*.(js|json|css|yaml)'",
    "postformat": "stylelint --fix '**/*.css'",
    "standard": "standard",
    "stylelint": "stylelint '**/*.css'"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "standard": "^17.0.0",
    "stylelint": "^14.11.0",
    "stylelint-config-standard": "^28.0.0"
  }
}
